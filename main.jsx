import React from 'react';
import ReactDOM from 'react-dom/client';
import DigitalHabitTracker from './Tracker';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DigitalHabitTracker />
  </React.StrictMode>
);
